[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qdrant-indexer"
version = "0.2.0"
description = "Qdrant vector indexing and search pipeline"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "pipeline-commons>=0.1.0",
    "qdrant-client>=1.7",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov",
]

[tool.uv.sources]
pipeline-commons = { workspace = true }
